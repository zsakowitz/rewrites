fn MyType(x: type) type {
    return x;
}

fn world(x: i32) i32 {
    return x;
}

fn id(T: type, x: T) T {
    return x;
}

test .{ world(45), world(32), MyType(i32), id(i32, 45), id(i32, 67), id(u32, 4) };
